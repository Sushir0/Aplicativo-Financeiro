package com.example.finance.lvl1

import org.junit.Assert
import org.junit.Test

class CategoriaUnitTest {
/*
    @Test
    fun testGerarCategoria(){
        //Arrenge
        LoginController().testeCadastro()


        //Act
        CategoriaDebbug().gerarCategoriasBasicas()

        //Assert
        val categorias = Login.getCasaLogada().getCategorias(afetaCasa = true, afetaPessoa = true)
        Assert.assertEquals(CategoriaDebbug().getSizeCategoriasBase(), categorias.size)
        CategoriaDebbug().categorias_Contas.forEach { nomeCategoria ->
            Assert.assertTrue(categorias.any { it.nome == nomeCategoria })
        }
        CategoriaDebbug().categorias_Recebimentos.forEach { nomeCategoria ->
            Assert.assertTrue(categorias.any { it.nome == nomeCategoria })
        }

    }



 */
}